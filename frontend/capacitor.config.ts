import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.bastion.app',
  appName: 'Bastion',
  webDir: 'dist/frontend/browser',
  android: {
    backgroundColor: '#1976d2'
  }
};

export default config;
